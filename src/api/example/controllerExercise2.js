/* eslint-disable no-empty */
module.exports = () => {
  const answer = async (request, reply) => {
    //your answer here
  };

  //SOLUTION
  /* const answer = async (request, reply) => {
    reply.code(200).send(`did you say ${request.body.Body}?`);
  };
 */
  return {
    answer
  };
};
