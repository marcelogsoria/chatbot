/* eslint-disable no-empty */
module.exports = () => {
  const answer = async (_, reply) => {
    // your answer here
  };

  // SOLUTION
  /* const answer = async (_, reply) => {
    reply.code(200).send('OK, received');
  }; */

  return {
    answer
  };
};
